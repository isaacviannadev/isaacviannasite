import styled from "styled-components";
import { Content, Section } from "../main/styled";

export const ContactSection = styled(Section)`
  min-height: 60rem;
`;

export const ContactContent = styled(Content)`
  gap: 10rem;
  h2 {
    max-width: 60rem;
  }
`;
